export enum PermissionModule {
  USERS = 'users',
  ROLES = 'roles',
  PERMISSIONS = 'permissions',
}

export enum PermissionAction {
  CREATE = 'create',
  READ = 'read',
  UPDATE = 'update',
  DELETE = 'delete',
  VIEW = 'view',
  DETAIL = 'detail',
}
